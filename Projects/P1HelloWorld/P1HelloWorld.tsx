import { StatusBar } from 'expo-status-bar';
import { StyleSheet, Text, View } from 'react-native';

export default function P1HelloWorld() {
    return (
        <View>
          <Text>Hello React Native</Text>
        </View>
      );
    }
    
    const styles = StyleSheet.create({

    });
    